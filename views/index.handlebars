<h1>BURGER TIME!!</h1>

<h2>Bugers to eat</h2>

<ul>
    {{#each burgers}}
    {{#unless devour}}
    {{> burgers/burger-block devour=true}}
    {{/unless}}
    {{/each}}
</ul>

<h2>Devoured burgers</h2>

<ul>
    {{#each burgers}}
    {{#if devour}}
    {{> burgers/burger-block devour=false}}
    {{/if}}
    {{/each}}
</ul>

<h2>Add a New Burger</h2>

<form class="create-form">

    <div class="form-group">
        <label for="burg">New Burger</label>
        <input type="text" id="burg" name="name" placeholder="type in burger here">
    </div>

    <div class="form-group">
        <label for="devour">Devour</label>
        <input type="radio" name="devour" value="1">Eaten</br>
        <input type="radio" name="devour" value="0" checked> Ready to eat!

        <button type="submit">Add this burger!</button>
    </div>
</form>