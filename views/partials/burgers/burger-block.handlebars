<li>
    {{burger_name}}

    <button class="eatBurger" data-id="{{id}}" data-devourstate="{{devoured}}" data-burgername="{{burger_name}}">
    {{#if devoured}}Cook Again{{else}}Eat DAT Burgah!!{{/if}}
    </button>

    <button class="trashBurger" data-id="{{this.id}}" data-burgername="{{burger_name}}">DELETE this burger</button>
</li>